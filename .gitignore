*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/

# Claude Code's own local state — not project source
/.claude/

# Signing material, if release signing is ever set up — never commit these
*.jks
*.keystore
!/app/debug.keystore
keystore.properties
