<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nord0"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="@string/widget_title_note"
        android:textColor="@color/nord8"
        android:textSize="12sp"
        android:textStyle="bold" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:scrollbars="none" />

        <!-- Shown (and made clickable via setEmptyView + its own PendingIntent) whenever the note
             is blank — the ListView goes GONE automatically in that state, so without a real
             sibling view here the whole rest of the widget beyond the title would be dead space
             that doesn't open the app, regardless of how large the widget is resized. -->
        <TextView
            android:id="@+id/empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="monospace"
            android:text="@string/widget_no_note"
            android:textColor="@color/nord3"
            android:textSize="14sp" />

    </FrameLayout>

</LinearLayout>
